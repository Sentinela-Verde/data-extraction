# Copie pra .env (nunca comitar o .env) e preencha os valores reais.

# Projeto do Google Cloud usado pra faturamento das queries no BigQuery
# (usado por api_ibge/). Ver api_ibge/README.md.
PROJECT_ID=

# Usado por outras coletas / integrações do projeto (se aplicável).
GOOGLE_MAPS_API_KEY=
